/* jshint -W069 */
module.exports = Snap

/**
 * Snapchat Snap
 *
 * @param {Object} params
 */
function Snap (params) {
  var self = this
  if (!(self instanceof Snap)) return new Snap(params)

  throw new Error("TODO")
}
